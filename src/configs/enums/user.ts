export enum Roles {
  ADMIN = "admin",
  EXECUTIVE = "executive",
  INSTRUCTOR = "instructor",
  STUDENT = "student",
  UNVERIFIED = "unverified",
}

export enum Genders {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
