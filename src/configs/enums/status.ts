export enum EnrollmentStatus {
  ACTIVE = "active",
  COMPLETED = "completed",
  DROPPED = "dropped",
}

export enum CertificationStatus {
  REGISTERED = "registered",
  PASSED = "passed",
  FAILED = "failed",
}
